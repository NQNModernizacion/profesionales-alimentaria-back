name: nest-template

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    image: nest-template:latest
    command: ["sh", "-c", "pnpm db:migrate && pnpm db:seed:prod && node dist/main"]
    env_file:
      - .env.docker
    environment:
      API_BASE_URL: http://localhost:3000
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    ports:
      - "3000:3000"
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: nest_template
      POSTGRES_USER: nest_template_user
      POSTGRES_PASSWORD: nest_template_password
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nest_template_user -d nest_template"]
      interval: 5s
      timeout: 5s
      retries: 20
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
